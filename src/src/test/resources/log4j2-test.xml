<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="warn" monitorInterval="30">
  
    <!-- Logging Properties -->
    <Properties>
        <Property name="LOG_PATTERN">%date{HH:mm:ss.SSS} %2tid %level{WARN=WRN, DEBUG=DBG, ERROR=ERR, TRACE=TRC, INFO=INF} %-40.40logger{1.1.*}: %msg%n</Property>
    </Properties>
      
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>
  
    <Loggers>
        <Root level="trace">
            <AppenderRef ref="Console"/>
        </Root>
  
    </Loggers>
  
</Configuration>